## Frontend (Prototype)

A React-based UI prototype exists in the `frontend` folder.

Due to hackathon time constraints, the working interface used for the demo
is a Streamlit application located in the backend.

Future versions will integrate the React frontend with the backend APIs.
